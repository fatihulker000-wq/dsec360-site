import {createClient} from "@supabase/supabase-js";
import {NextRequest,NextResponse} from "next/server";
import {cookies} from "next/headers";
import {readMedulaSession} from "@/lib/medula/session";
import {callMedula} from "@/lib/medula/client";
export const runtime="nodejs";export const dynamic="force-dynamic";
function s(v:any){return String(v??"").trim()}
function db(){return createClient(process.env.SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}})}
async function ctx(){const c=await cookies();const auth=c.get("dsec_admin_auth")?.value||c.get("dsec_user_auth")?.value;const role=s(c.get("dsec_admin_role")?.value||c.get("dsec_user_role")?.value);const companyId=s(c.get("dsec_company_id")?.value);if(auth!=="ok"||!["super_admin","admin","company_admin"].includes(role))return null;return{role,companyId,scoped:role==="company_admin",simulation:c.get("dsec_medula_sim")?.value==="1",scenario:s(c.get("dsec_medula_sim_scenario")?.value||"SUCCESS")}}
type RouteContext={params:Promise<{id:string}>};
function simulatedFailure(scenario:string){if(scenario==="SEND_ERROR")return NextResponse.json({success:false,simulation:true,resultCode:"SIM-422",resultMessage:"Simülasyon: MEDULA iş kuralı hatası."},{status:422});if(scenario==="TIMEOUT")return NextResponse.json({success:false,simulation:true,error:"Simülasyon: MEDULA bağlantı zaman aşımı."},{status:504});if(scenario==="AUTH_ERROR")return NextResponse.json({success:false,simulation:true,error:"Simülasyon: MEDULA hekim oturumu/yetki hatası."},{status:401});return null}

function trDate(v:any){const d=new Date(v);if(Number.isNaN(d.getTime()))return"";return `${String(d.getDate()).padStart(2,"0")}.${String(d.getMonth()+1).padStart(2,"0")}.${d.getFullYear()}`}
export async function POST(_:NextRequest,context:RouteContext){try{const a=await ctx();if(!a)return NextResponse.json({success:false,error:"Yetkisiz erişim."},{status:401});const{id}=await context.params;const supabase=db();let q=supabase.from("health_prescriptions").select("*,health_prescription_items(*)").eq("id",id).eq("is_active",true);if(a.scoped)q=q.eq("company_id",a.companyId);const{data:p,error}=await q.single();if(error||!p)return NextResponse.json({success:false,error:"Reçete bulunamadı."},{status:404});const items=p.health_prescription_items||[];const missing:string[]=[];if(!s(p.patient_identity_number))missing.push("Hasta T.C.");if(!s(p.facility_code))missing.push("Tesis kodu");if(!p.provision_type)missing.push("Provizyon tipi");if(!p.prescription_date)missing.push("Reçete tarihi");if(!p.prescription_type)missing.push("Reçete türü");if(!p.prescription_subtype)missing.push("Reçete alt türü");if(!s(p.protocol_no))missing.push("Protokol no");if(!s(p.doctor_branch_code))missing.push("Doktor branş kodu");if(!s(p.doctor_certificate_code))missing.push("Doktor sertifika kodu");if(!s(p.doctor_identity_number))missing.push("Hekim T.C.");if(!s(p.diagnosis_code))missing.push("Tanı");if(items.length===0)missing.push("İlaç");for(const[i,x]of items.entries()){if(!s(x.barcode))missing.push(`İlaç #${i+1} barkod`);if(!x.quantity)missing.push(`İlaç #${i+1} adet`);if(x.dose1==null)missing.push(`İlaç #${i+1} doz1`);if(x.dose2==null)missing.push(`İlaç #${i+1} doz2`);if(x.usage_period==null)missing.push(`İlaç #${i+1} kullanım periyodu`);if(x.usage_period_unit==null)missing.push(`İlaç #${i+1} periyot birimi`)}if(missing.length)return NextResponse.json({success:false,error:"MEDULA gönderimi için eksik alanlar var.",missing,simulation:a.simulation},{status:400});
if(a.simulation){const fail=simulatedFailure(a.scenario);if(fail){await supabase.from("health_prescription_audit_logs").insert({prescription_id:id,company_id:p.company_id,employee_id:p.employee_id,action:"MEDULA_SIMULATION_ERROR",old_status:p.medula_status||"READY",new_status:p.medula_status||"READY",actor_role:a.role,note:`Simülasyon senaryosu: ${a.scenario}`,metadata:{simulation:true,scenario:a.scenario,externalNetwork:false}});return fail}const no=`SIM-${new Date().toISOString().slice(0,10).replaceAll("-","")}-${id.replaceAll("-","").slice(0,8).toUpperCase()}`;const now=new Date().toISOString();await supabase.from("health_prescriptions").update({medula_status:"SENT",e_prescription_no:no,medula_environment:"TEST",medula_last_result_code:"SIM-0000",medula_last_result_message:"Simülasyon başarılı: reçete SGK/MEDULA'ya gönderilmedi.",medula_last_warning_message:"TEST SIMULATION ONLY",medula_last_checked_at:now,medula_last_sent_at:now,updated_at:now}).eq("id",id);await supabase.from("health_prescription_audit_logs").insert({prescription_id:id,company_id:p.company_id,employee_id:p.employee_id,action:"MEDULA_SIMULATED_SENT",old_status:p.medula_status||"READY",new_status:"SENT",actor_role:a.role,note:"Simülasyon gönderimi başarılı. Dış ağ çağrısı yapılmadı.",metadata:{simulation:true,scenario:a.scenario,externalNetwork:false,ereceteNo:no}});return NextResponse.json({success:true,simulation:true,externalNetwork:false,resultCode:"SIM-0000",resultMessage:"Simülasyon başarılı. SGK/MEDULA'ya gerçek gönderim yapılmadı.",warningMessage:"Bu e-Reçete numarası yalnız test simülasyonuna aittir.",ePrescriptionNo:no,environment:"TEST"})}
const session=await readMedulaSession();if(!session)return NextResponse.json({success:false,error:"Önce MEDULA hekim oturumu açılmalıdır."},{status:401});if(s(p.doctor_identity_number)!==session.doctorTc)return NextResponse.json({success:false,error:"Reçete hekimi ile MEDULA oturumu hekimi aynı değil."},{status:409});if(s(p.facility_code)!==session.facilityCode)return NextResponse.json({success:false,error:"Reçete tesis kodu ile MEDULA oturum tesis kodu aynı değil."},{status:409});const ereceteDVO={tesisKodu:Number(p.facility_code),tcKimlikNo:Number(p.patient_identity_number),provizyonTipi:Number(p.provision_type),receteTarihi:trDate(p.prescription_date),receteTuru:Number(p.prescription_type),receteAltTuru:Number(p.prescription_subtype),protokolNo:s(p.protocol_no),doktorTcKimlikNo:Number(session.doctorTc),doktorBransKodu:Number(p.doctor_branch_code),doktorSertifikaKodu:Number(p.doctor_certificate_code),ereceteIlacListesi:items.map((x:any)=>({barkod:Number(x.barcode),adet:Number(x.quantity),kullanimSekli:x.usage_form==null?undefined:Number(x.usage_form),kullanimDoz1:Number(x.dose1),kullanimDoz2:Number(x.dose2),kullanimPeriyot:Number(x.usage_period),kullanimPeriyotBirimi:Number(x.usage_period_unit),geriOdemeKapsaminda:s(x.reimbursement_flag||"E")})),ereceteTaniListesi:[{taniKodu:s(p.diagnosis_code),taniAdi:s(p.diagnosis_name)||undefined}]};const result=await callMedula(session,"ereceteGiris",{tesisKodu:Number(session.facilityCode),doktorTcKimlikNo:Number(session.doctorTc),ereceteDVO});const raw:any=result.data;const find=(o:any,k:string):any=>{if(!o||typeof o!=="object")return undefined;if(k in o)return o[k];for(const v of Object.values(o)){const r=find(v,k);if(r!==undefined)return r}};const medulaNo=s(find(raw,"ereceteNo"));const patch:any={medula_last_result_code:result.resultCode,medula_last_result_message:result.resultMessage||null,medula_last_warning_message:result.warningMessage||null,medula_last_checked_at:new Date().toISOString(),medula_environment:session.environment,medula_status:result.ok?"SENT":"ERROR",updated_at:new Date().toISOString()};if(result.ok){patch.medula_last_sent_at=new Date().toISOString();if(medulaNo)patch.e_prescription_no=medulaNo}await supabase.from("health_prescriptions").update(patch).eq("id",id);await supabase.from("health_prescription_audit_logs").insert({prescription_id:id,company_id:p.company_id,employee_id:p.employee_id,action:result.ok?"MEDULA_SENT":"MEDULA_ERROR",old_status:p.medula_status||"READY",new_status:patch.medula_status,actor_role:a.role,note:result.resultMessage||result.warningMessage||null,metadata:{environment:session.environment,resultCode:result.resultCode,ereceteNo:medulaNo||null,simulation:false}});return NextResponse.json({success:result.ok,resultCode:result.resultCode,resultMessage:result.resultMessage,warningMessage:result.warningMessage,ePrescriptionNo:medulaNo||null,environment:session.environment,simulation:false},{status:result.ok?200:422})}catch(e:any){return NextResponse.json({success:false,error:e?.message||"MEDULA gönderimi başarısız."},{status:502})}}
